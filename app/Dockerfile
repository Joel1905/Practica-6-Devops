# Usar una imagen base oficial de Nginx
FROM nginx:alpine

# Copiar el archivo HTML de la aplicación al contenedor
COPY index.html /usr/share/nginx/html/index.html

# Exponer el puerto 80 para que la aplicación sea accesible desde fuera
EXPOSE 80
